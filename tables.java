import java.util.Scanner;
class tables{
    public static void main(String args[])
    {
        Scanner scan= new Scanner(System.in);
        // System.out.println("enter the numbers")
        // int number = scan.nextInt();
        System.out.println("enter the table number which tables you want to print");
        int tables = scan.nextInt();
        
        System.out.println("enter the numbers");
        int number = scan.nextInt();
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
        if(number<=10)
        {
            System.out.println(tables + " x " + number + " = " + (tables * number));
            number++;

        }
    }
}